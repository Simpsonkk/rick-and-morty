import { createSlice, PayloadAction } from '@reduxjs/toolkit';

// import { AuthorizationStatus, NameSpace } from '../../../consts';
// import { UserProcessState } from '../../../types/state.type';

// const initialState: UserProcessState = {
//   AuthorizationStatus: AuthorizationStatus.Unknown,
// };

// export const userProcess = createSlice({
  // name: NameSpace.User,
  // initialState,
  // reducers: {
  //   loadAuthorizationStatus: (state, action: PayloadAction<AuthorizationStatus>) => {
  //     state.AuthorizationStatus = action.payload;
  //   },
  // },
// });

// export const { loadAuthorizationStatus } = userProcess.actions;
